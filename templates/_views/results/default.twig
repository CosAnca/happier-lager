{#
 # Result View Template – Default
 # -------------------------------
 #
 # This template gets loaded whenever the Result Router falls back to the default variant.
 #
-#}

{% block view_main %}
  <div class="g1-flex4 g2-flex6 g3-flex8 g3-offset2">
    <span class="post-date">{{ entry.postDate.format('m.d.Y') }}</span>
    <h2 class="beta">{{ entry.getLink() }}</h2>
  </div>
{% endblock %}